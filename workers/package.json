{
  "name": "prompted-blog-workers",
  "private": true,
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "validate": "tsc --noEmit && npm test"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.61",
    "@cloudflare/workers-types": "^4.20250809.0",
    "@vitest/ui": "^3.2.4",
    "vitest": "^3.2.4",
    "wrangler": "^4.28.1"
  },
  "dependencies": {
    "oauth4webapi": "^3.7.0"
  }
}
